# 1. Берем легкий образ Python
FROM python:3.11-slim

# 2. Отключаем буферизацию вывода (чтобы логи видеть сразу)
ENV PYTHONUNBUFFERED=1

# 3. Создаем рабочую папку внутри контейнера
WORKDIR /app

# 4. Копируем файл зависимостей
COPY requirements.txt .

# 5. Устанавливаем библиотеки
RUN pip install --no-cache-dir -r requirements.txt

# 6. Копируем весь код
COPY . .

# 7. Говорим, какую команду запустить при старте контейнера
# --host 0.0.0.0 делает сервер доступным извне контейнера
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]